%h2 Sign in
= form_tag('/auth') do
  = text_field_tag("auth[email]", nil, placeholder: 'Email Address')
  = password_field_tag("auth[password]", nil, placeholder: 'Password')
  = submit_tag("Sign In")
